# npm configuration
# Use --omit=dev instead of legacy production flag
omit=dev
# Disable fund messages for cleaner output
fund=false
# Set registry for consistency
registry=https://registry.npmjs.org/ 